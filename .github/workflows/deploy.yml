name: Deploy to GitHub Pages

on:
  push:
    branches: 'main'
  workflow_dispatch:

jobs:
  get_pages_url_job:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      pages_url: ${{ steps.get_url.outputs.PAGES_URL }}
    steps:
      - name: Get GitHub Pages URL
        id: get_url
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if [ -z "$ACT" ]; then
            PAGES_URL=$(gh api "repos/${{ github.repository }}/pages" --jq '.html_url')
            echo "PAGES_URL=$PAGES_URL" >> "$GITHUB_OUTPUT"
          else
            echo "PAGES_URL=rrmistry.ca" >> "$GITHUB_OUTPUT"
          fi

  build_and_test:
    needs: [get_pages_url_job]
    runs-on: ubuntu-latest
    container:
      image: mcr.microsoft.com/playwright:v1.50.1-noble
      options: --user 1001
    env:
      DEV_SERVER_PORT: 5173
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # - uses: actions/setup-node@v4
      #   if: ${{ !env.ACT }}
      #   with:
      #     node-version: lts/*
      #     cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Sync SvelteKit
        run: npx svelte-kit sync

      - name: Run unit tests
        run: npm run test:unit

      - name: build
        env:
          BASE_PATH: '/'
        run: npm run build

      - name: Start SvelteKit dev server and download sitemap.xml
        run: |
          # Start dev server in background
          npm run dev -- --port $DEV_SERVER_PORT &
          SERVER_PID=$!
          
          # Wait for server to be ready
          for i in $(seq 1 30); do
            if curl -s http://localhost:$DEV_SERVER_PORT/ > /dev/null; then
              echo "Server is ready!"
              break
            fi
            echo "Waiting for server to start... attempt $i/30"
            sleep 2
          done
          
          # Download sitemap.xml
          for i in $(seq 1 10); do
            if curl http://localhost:$DEV_SERVER_PORT/sitemap.xml -o build/sitemap.xml; then
              echo "Successfully downloaded sitemap.xml"
              break
            fi
            echo "Attempt $i failed, waiting..."
            sleep 5
          done
          
          # Kill the server and wait for it to stop
          echo "Stopping dev server..."
          kill $SERVER_PID || true
          
          # Wait for the port to be free
          for i in $(seq 1 10); do
            if ! curl -s http://localhost:$DEV_SERVER_PORT/ > /dev/null 2>&1; then
              echo "Server stopped successfully"
              break
            fi
            echo "Waiting for server to stop... attempt $i/10"
            sleep 1
          done
        working-directory: .

      - name: Replace sitemap.xml domain
        run: sed -i 's|http://localhost:${{ env.DEV_SERVER_PORT }}|${{ needs.get_pages_url_job.outputs.pages_url }}|g' build/sitemap.xml

      - name: Generate SEO files
        run: node scripts/generate-seo-files.mjs ${{ needs.get_pages_url_job.outputs.pages_url }}

      - name: Upload Artifacts
        if: ${{ !env.ACT }} # skip during local actions testing
        uses: actions/upload-pages-artifact@v3
        with:
          # this should match the `pages` option in your adapter-static options
          path: 'build/'

      - name: Run Playwright tests
        run: npm run test:e2e

      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() && !env.ACT }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

  deploy:
    needs: [build_and_test]
    # needs: build_and_test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    permissions:
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
